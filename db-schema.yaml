
user:
    description: "user id"
    type: "string"

password:
    description: "password"
    type: "string"
email:
    description: "user email"
    type: "string"
geometry:
    description:  "geojson polygon geometry which will be used as a spatial filter"
    type: "geojson geometry"

products_bucket_list:
    description:  "list pf the previous downloaded products"
    type: "list"

last_product_date:
    description:  "time when last product was downloaded"
    type: "dateTime"